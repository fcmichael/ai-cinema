export class Event {
  constructor(
    public id: number,
    public title: string,
    public eventDate: string,
    public shortDescription: string,
    public description: string,
    public image: any,
  ) {
  }
}
